# Cloudflare Worker endpoint that receives contact form submissions.
# The worker accepts a JSON POST and stores/forwards the lead payload.
# Without this variable the ContactWizard falls back to a console.log mock.
NEXT_PUBLIC_LEAD_ENDPOINT=
